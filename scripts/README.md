# Scripts

- `check_pdf_fonts.sh`: verifies Vazirmatn presence and font embedding in a candidate PDF.

Full visual QA cannot be replaced by a script. Rendering and vision inspection of **all pages** remains mandatory according to `MASTER_PDF_PROMPT.md`.
